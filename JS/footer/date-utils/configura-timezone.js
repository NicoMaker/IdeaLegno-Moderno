// ---- estratto da JS/footer/date-utils.js (righe 27-33) ----
function configuraTimezone(data) {
  if (data && data.timezone) {
    _shopTimezone = data.timezone;
  }
}

// Componenti "da orologio da muro" dell'attività per un dato istante
