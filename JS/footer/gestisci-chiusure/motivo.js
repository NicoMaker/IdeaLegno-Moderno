// ---- estratto da JS/footer/Gestisci_chiusure.js (righe 44-49) ----
function _motivo(voce) {
  return voce.motivo && voce.motivo.trim() ? voce.motivo.trim() : "Ferie";
}

// ── Costruisce Set date + Map data→motivo da "chiusure" ──────

